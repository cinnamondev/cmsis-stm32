{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "pwsh",
            "label": "environment-bootstrap",
            "args": [
                "./bootstrap.ps1",
            ]
        },
        {
            "type": "shell",
            "dependsOn": ["Buildgen: Build [DEBUG]"],
            "dependsOrder":"sequence",
            "command": "openocd -f interface/cmsis-dap.cfg -f target/rp2040.cfg -f ./openocd.cfg -c “program ./build/picopico/Debug/out/picopico.elf verify reset exit”",
            "label": "Build & Program DevBrd",

        },
        {
            "type": "shell",
            "command": "openocd -f interface/cmsis-dap.cfg -f target/rp2040.cfg -f ./openocd.cfg -c “program ./build/picopico/Debug/out/picopico.elf verify reset exit”",
            "label": "Program DevBrd",
        },
        {
            "type": "shell",
            "command": "openocd -f ./openocd.cfg -c “init ; reset halt ; stm32f4x.cpu arp_reset assert 0 ; exit”",
            "label": "Reset uC",
        }
    ]
}